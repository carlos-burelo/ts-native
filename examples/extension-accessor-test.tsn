class Counter {
    value: int = 0
}

extension CounterAccess on Counter {
    get doubled(): int {
        return this.value * 2
    }

    set doubled(next: int) {
        this.value = next / 2
    }
}

const c = new Counter()
c.doubled = 10

print(c.value)
print(c.doubled)
