extension StringMethodValues on str {
    shout(): str {
        return this + "!"
    }

    async sizeAsync(): int {
        return this.length
    }
}

async function main(): int {
    const shout = "hi".shout
    print(shout())

    const size = "hello".sizeAsync
    return await size()
}

print(await main())
