/**
 * Interface Test
 */

interface Named {
    name: str
}

interface Aged {
    age: int
}

const named: Named = {
    name: "John"
}

const aged: Aged = {
    age: 30
}

// Test access
const n: str = named.name
const a: int = aged.age
