/**
 * Interface Implementation Error Cases
 */

interface Named {
    getName(): str
    getAge(): int
}

// ERROR: Missing getAge method
class User implements Named {
    name: str

    constructor(name: str) {
        this.name = name
    }

    getName(): str {
        return this.name
    }
}

const user = new User("Alice")
