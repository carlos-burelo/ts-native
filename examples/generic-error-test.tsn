/**
 * Generic Error Test
 */

function identity<T>(value: T): T {
    return value
}

// ERROR: Should fail - passing wrong type
const num: str = identity<int>(42)

// ERROR: Should fail - wrong number of type arguments
const x = identity<int, str>(10)
