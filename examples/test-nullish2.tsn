let x: str? = "hello"

// Test 1: just ??
let r1 = x ?? "default"
print(r1)  // should be "hello"

// Test 2: comparison
let r2 = x === "hello"
print(r2)  // should be true

// Test 3: ?? with comparison
let r3 = x ?? "default" === "hello"
print(r3)  // should be true
