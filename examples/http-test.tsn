import { Method, RequestInit, HttpMethod } from "std:http";

let myMethod: HttpMethod = Method.POST;

let requestOptions: RequestInit = {
    method: myMethod,
    headers: { "Content-Type": "application/json" },
    body: '{"foo": "bar"}',
    timeout: 5000
};

print(requestOptions);
