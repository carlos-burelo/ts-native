class Circle {
    radius: int

    constructor(r: int) {
        this.radius = r
    }

    getRadius(): int {
        return this.radius
    }
}

const c = new Circle(5);
print(c.getRadius());
print(new Circle(10).getRadius());

const arrow = (x: int) => x * 2;
arrow(10);
