[package]
name = "tsn-types"
version = "0.1.0"
edition = "2021"

[dependencies]
parking_lot = "0.12.5"
tsn-core = { path = "../tsn-core" }
rust_decimal = { version = "1", features = ["serde"] }
serde = { version = "1", features = ["derive", "rc"] }
